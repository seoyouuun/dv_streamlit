import streamlit as st

# 1. 페이지 기본 설정 (탭 이름, 아이콘 등)
st.set_page_config(
    page_title="여시코껭이의 대시보드",
    page_icon="🎈",
    layout="wide"
)

# 2. 메인 화면 헤더
st.title("🏠 메인 홈")
st.markdown("""
반가워요! 여기는 **메인 페이지**입니다.  
왼쪽 **사이드바**에서 페이지를 선택하거나, 아래 **버튼**을 눌러 이동해보세요.
""")

st.divider() # 구분선

# 3. 서브페이지 안내 및 바로가기 (Streamlit 1.31 이상 버전 필요)
st.subheader("📂 메뉴 바로가기")

col1, col2 = st.columns(2)

with col1:
    st.info("📊 데이터 분석을 보고 싶다면?")
    # 주의: page 경로는 실제 pages 폴더 안의 파일명과 똑같아야 합니다!
    # 예: pages/01_subpage.py 라면 -> page="pages/01_subpage.py"
    # 파일명이 확실하지 않으면 이 부분은 주석 처리하거나 빼도 됩니다.
    if st.button("첫 번째 페이지로 이동"):
        st.switch_page("pages/서브페이지파일명1.py") 

with col2:
    st.success("📝 결과를 확인하려면?")
    if st.button("두 번째 페이지로 이동"):
        st.switch_page("pages/서브페이지파일명2.py")

# 4. 꾸미기용 (옵션)
st.divider()
st.caption("Created by 여시코껭이 서여이 | Streamlit Project")
